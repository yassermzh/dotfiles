set icons true
set hidden false

map <delete> delete

cmd open ${{
    firefox "$f" & disown
}}

map <enter> open
map gD cd ~/Downloads
map gw cd ~/works

map DD delete

echo "Loading ~/.config/lf/lfrc"

